<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>IsLocked__c</fullName>
    <formula>AND(ISPICKVAL(Status, &apos;Invoiced&apos;),
OR(ISBLANK(InvoicedDate__c),
IF(DAY(TODAY()) &gt; 4,
InvoicedDate__c &lt; (TODAY() - DAY(TODAY()) + 1),
InvoicedDate__c &lt; ADDMONTHS((TODAY() - DAY(TODAY()) + 1), -1)
)
)
)</formula>
    <label>Locked?</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
