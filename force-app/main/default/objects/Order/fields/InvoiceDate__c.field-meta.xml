<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>InvoiceDate__c</fullName>
    <formula>If(isblank(If(isblank(Inv_Serv_Note_Date__c),F_Delivery_Date__c, 
if(Delivery_Date__c &lt;TODAY()-DAY(TODAY())+1, TODAY()-DAY(TODAY())+1,Inv_Serv_Note_Date__c)   ))
,Requested_Delivery_Date__c,If(isblank(Inv_Serv_Note_Date__c),F_Delivery_Date__c,
if(Delivery_Date__c &lt;TODAY()-DAY(TODAY())+1, TODAY()-DAY(TODAY())+1,Inv_Serv_Note_Date__c)  ))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>InvoiceDate</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
